import IconFont from '../IconFont'
import styled from 'styled-components'

import { $font_size_large } from "../../common/variable";

export default styled(IconFont)`
  font-size: ${$font_size_large};
  line-height: 44px;
`
